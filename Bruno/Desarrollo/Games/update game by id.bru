meta {
  name: update game by id
  type: http
  seq: 4
}

patch {
  url: http://localhost:3000/api/games/:id
  body: json
  auth: inherit
}

params:path {
  id: 
}

body:json {
  {
    "name": "string"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
