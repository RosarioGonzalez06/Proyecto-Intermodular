meta {
  name: update developer by id
  type: http
  seq: 4
}

patch {
  url: http://localhost:3000/api/developers/:id
  body: json
  auth: inherit
}

params:path {
  id: 
}

body:json {
  {
    "name": "string"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
