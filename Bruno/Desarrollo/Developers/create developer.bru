meta {
  name: create developer
  type: http
  seq: 2
}

post {
  url: http://localhost:3000/api/developers
  body: json
  auth: inherit
}

body:json {
  {
    "name": "string"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
